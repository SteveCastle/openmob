import React from "react"
import PropTypes from "prop-types"

const CreateACL = ({id}) => (
  <div>
    <h1>Create ACL</h1>
  </div>
)

CreateACL.propTypes = {
  id: PropTypes.string
}

export default CreateACL

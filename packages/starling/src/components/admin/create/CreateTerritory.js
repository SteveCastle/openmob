import React from 'react'
import PropTypes from 'prop-types'

const CreateTerritory = ({ id }) => (
  <div>
    <h1>Create Territory</h1>
  </div>
)

CreateTerritory.propTypes = {
  id: PropTypes.string,
}

export default CreateTerritory

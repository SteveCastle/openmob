import React from 'react'
import PropTypes from 'prop-types'

const CreateFollower = ({ id }) => (
  <div>
    <h1>Create Follower</h1>
  </div>
)

CreateFollower.propTypes = {
  id: PropTypes.string,
}

export default CreateFollower

import React from "react"
import PropTypes from "prop-types"

const EditPollMembership = () => (
  <div>
    <h1>Edit PollMembership</h1>
  </div>
)

EditPollMembership.propTypes = {
    id: PropTypes.string
}

export default EditPollMembership

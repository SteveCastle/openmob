import React from "react"
import PropTypes from "prop-types"

const EditMailingAddress = () => (
  <div>
    <h1>Edit MailingAddress</h1>
  </div>
)

EditMailingAddress.propTypes = {
    id: PropTypes.string
}

export default EditMailingAddress

import React from 'react'
import PropTypes from 'prop-types'

const EditEmailAddress = () => (
  <div>
    <h1>Edit EmailAddress</h1>
  </div>
)

EditEmailAddress.propTypes = {
  id: PropTypes.string,
}

export default EditEmailAddress

import React from 'react'
import PropTypes from 'prop-types'

const EditLayoutType = () => (
  <div>
    <h1>Edit LayoutType</h1>
  </div>
)

EditLayoutType.propTypes = {
  id: PropTypes.string,
}

export default EditLayoutType

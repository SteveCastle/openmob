import React from "react"
import PropTypes from "prop-types"

const EditNote = () => (
  <div>
    <h1>Edit Note</h1>
  </div>
)

EditNote.propTypes = {
    id: PropTypes.string
}

export default EditNote

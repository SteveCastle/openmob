import React from "react"
import PropTypes from "prop-types"

const EditField = () => (
  <div>
    <h1>Edit Field</h1>
  </div>
)

EditField.propTypes = {
    id: PropTypes.string
}

export default EditField

import React from 'react'
import PropTypes from 'prop-types'

const EditLayoutColumn = () => (
  <div>
    <h1>Edit LayoutColumn</h1>
  </div>
)

EditLayoutColumn.propTypes = {
  id: PropTypes.string,
}

export default EditLayoutColumn

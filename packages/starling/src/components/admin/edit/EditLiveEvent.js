import React from 'react'
import PropTypes from 'prop-types'

const EditLiveEvent = () => (
  <div>
    <h1>Edit LiveEvent</h1>
  </div>
)

EditLiveEvent.propTypes = {
  id: PropTypes.string,
}

export default EditLiveEvent

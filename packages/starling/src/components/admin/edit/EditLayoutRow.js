import React from 'react'
import PropTypes from 'prop-types'

const EditLayoutRow = () => (
  <div>
    <h1>Edit LayoutRow</h1>
  </div>
)

EditLayoutRow.propTypes = {
  id: PropTypes.string,
}

export default EditLayoutRow

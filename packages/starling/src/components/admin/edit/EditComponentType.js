import React from "react"
import PropTypes from "prop-types"

const EditComponentType = () => (
  <div>
    <h1>Edit ComponentType</h1>
  </div>
)

EditComponentType.propTypes = {
    id: PropTypes.string
}

export default EditComponentType

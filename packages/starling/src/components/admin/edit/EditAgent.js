import React from "react"
import PropTypes from "prop-types"

const EditAgent = () => (
  <div>
    <h1>Edit Agent</h1>
  </div>
)

EditAgent.propTypes = {
    id: PropTypes.string
}

export default EditAgent

import React from 'react'
import PropTypes from 'prop-types'

const EditIssue = () => (
  <div>
    <h1>Edit Issue</h1>
  </div>
)

EditIssue.propTypes = {
  id: PropTypes.string,
}

export default EditIssue

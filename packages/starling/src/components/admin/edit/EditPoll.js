import React from "react"
import PropTypes from "prop-types"

const EditPoll = () => (
  <div>
    <h1>Edit Poll</h1>
  </div>
)

EditPoll.propTypes = {
    id: PropTypes.string
}

export default EditPoll

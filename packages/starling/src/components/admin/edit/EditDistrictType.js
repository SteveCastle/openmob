import React from "react"
import PropTypes from "prop-types"

const EditDistrictType = () => (
  <div>
    <h1>Edit DistrictType</h1>
  </div>
)

EditDistrictType.propTypes = {
    id: PropTypes.string
}

export default EditDistrictType

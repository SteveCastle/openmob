import React from "react"
import PropTypes from "prop-types"

const EditPhoto = () => (
  <div>
    <h1>Edit Photo</h1>
  </div>
)

EditPhoto.propTypes = {
    id: PropTypes.string
}

export default EditPhoto

import React from "react"
import PropTypes from "prop-types"

const Edit{{.Table.Name}} = () => (
  <div>
    <h1>Edit {{.Table.Name}}</h1>
  </div>
)

Edit{{.Table.Name}}.propTypes = {
    id: PropTypes.string
}

export default Edit{{.Table.Name}}
